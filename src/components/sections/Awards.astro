---
import Section from "../Section.astro"

import { awards } from "@cv"
---

<Section title="Reconocimientos y Premios">
  <ul>
    {
      awards.map(({ title, year, awarder }) => {    

        return (
          <li>
            <article>
              <header>
                <div>
                  <h3>{awarder}</h3>
                </div>

                <time>{year}</time>
              </header>

              <footer>
                <p>{title}</p>
              </footer>
            </article>
          </li>
        )
      })
    }
  </ul>
</Section>

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  article h3 {
    font-weight: 500;
    color: #777;
  }

  header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 4px;
  }

  time {
    color: #fff;
    font-size: 0.85rem;
    min-width: 102px;
  }

  @media (width <= 700px) {
    time {
      text-align: right;
    }
  }
</style>